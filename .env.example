# Prefer a short-lived authorization token. Never commit real values.
GRAVITY_AUTH_TOKEN=

# Optional username/password refresh flow.
GRAVITY_USERNAME=
GRAVITY_PASSWORD=

# Optional checkout root when running data-product commands from an installed wheel.
GRAVITY_SDK_HOME=
