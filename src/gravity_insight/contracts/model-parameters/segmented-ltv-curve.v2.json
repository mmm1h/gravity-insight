{
  "artifact_kind": "model_parameters",
  "schema_version": "gravity.model-parameters.v1",
  "model_uri": "model://gravity/segmented-ltv-curve@2",
  "revision": 1,
  "operator_uri": "operator://gravity/scenario-projection@2",
  "mode": "caller_bound_parameters",
  "required_parameters": [
    "frozen segment baseline",
    "bounded curve coefficients",
    "safe horizon",
    "explicit mode, value scopes and evidenced disjoint aggregation axis"
  ],
  "limitations": [
    "Project values are supplied through governed Semantic and Context bindings.",
    "The artifact validates deterministic scenario mechanics, not project predictive accuracy.",
    "Aggregate scenarios require additive or permitted semi-additive components; per-user LTV ratios must use rowwise mode."
  ]
}
