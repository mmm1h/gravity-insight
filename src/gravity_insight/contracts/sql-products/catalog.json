{
  "schema_version": 3,
  "product_kinds": {
    "custom-sql": {
      "purpose": "workspace-owned governed aggregate query",
      "privacy": "aggregate output only",
      "required_placeholders": [
        "app_ids",
        "start",
        "end",
        "limit"
      ],
      "output_policy": "project only declared output_fields and reject user-level field names",
      "max_rows": 10000
    }
  }
}
