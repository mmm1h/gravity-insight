<!-- generated by scripts/generate_agent_skills.py; do not edit -->
# 把某一天的用户明细导出成文件

问法用「把某一天的用户明细导出成文件并下载」或「用户明细导出」，命中 `export.analysis.user_detail.start`。宽问「导出」不是这一张卡。
```powershell
gravity export describe export.analysis.user_detail.start
```
先 `gravity run analysis.user_detail.list` 同一 App、同一天非空，再执行：
```powershell
gravity export run export.analysis.user_detail.start --input <request.json> --columns ClientID,CreateTime --idempotency-key <unique-key> --output <writable-file.xlsx> --timeout 300
```
`--columns` 填请求代码 `ClientID,CreateTime`，不要填文件表头 `客户ID,注册时间`。

## 预期 envelope 形状

```json
{
  "completion_status": "<str>",
  "file": {
    "rows": "<int>"
  },
  "ok": "<bool>",
  "operation_id": "<str>",
  "schema_version": "<str>",
  "status": "<str>"
}
```

## 失败分支

- exit 0: success, including an allowed empty result.
- exit 2: caller input or authentication setup.
- exit 3: upstream, permission, or rate limit.
- exit 4: local contract, privacy, policy, or I/O.

## 下一步

看 `completion_status`：`complete` 是原子提交且 `file.rows` 等于预检 `total_items`；`truncated` 是触顶截断并给出已知总量；`partial` 不是完整导出。父读取信封 `truncated=true` 只表示没拉完全部分页。单日 `file.rows` = `list.total_items` = 当天漏斗第一步。
